/**
 * Returns the sum of two numbers
 * @since 0.0
 * @version 1.0.1
 * @param num1 the first number
 * @param num2 the second number
 * @returns num1 + num2
 */
export function suma(num1 = 0, num2 = 0): number {
    const result = num1 + num2;
    console.log(result);
    return result;
}
